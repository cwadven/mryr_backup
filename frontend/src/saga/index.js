import {all,fork} from 'redux-saga/effects';

// import [sagaName] from '[sagaLocation]';

export default function* rootSaga(){
    //put fork(sagaName) in all array
    yield all([
        
    ]);
}